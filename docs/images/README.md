# Images

Drop screenshots and recordings for the main `README.md` here.

Expected files:

| File | What it should show |
| --- | --- |
| `demo.gif` | A 10–15 second loop: pick a model, type a sentence, see the tags and the extracted expression. |
| `sentence-page.png` | The single-sentence page with a result on screen. |
| `batch-page.png` | The batch page after an upload, showing the results table. |

Keep `demo.gif` under about 5 MB so it loads quickly on the repository page.
Recommended capture width is 1280px.
